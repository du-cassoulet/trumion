const Command = require("../classes/Command");

module.exports = new Command({
  name: "refresh-slash",
  aliases: ["rs"],
  description: "HELP_REFRESH_SLASH",
  options: [],
  execute: async function({ message }) {

  }
});